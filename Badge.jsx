Icon-only square button for toolbars, close affordances, and carousel arrows.

```jsx
<IconButton label="Call us" variant="accent"><PhoneIcon/></IconButton>
```

Variants: `default` (white + outline), `dark`, `accent` (red). Always pass `label`.
